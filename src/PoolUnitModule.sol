// SPDX-License-Identifier: UNLICENSED
pragma solidity 0.8.13;

import "./Storage.sol";

// /// @notice module contract for the pool unit logic
// /// @dev only internal functions in modules
// library PoolUnitModule is Storage {
//     /// @notice identifier of pool unit as the 3 parameters
//     /// @notice that make a poolUnit unique in the system
//     /// @member durationIndex index of the duration in DURATIONS[]
//     /// @member startDate timestamp after which the pool can be borrowed from
//     /// @member asset address of the ERC721 / contract handling the asset
//     struct PoolUnitId {
//         uint8 durationIndex;
//         uint256 startDate;
//         address asset;
//     }

//     function supply(PoolUnitId calldata id) internal {

//     }
// }

/// @notice module library for the pool unit logic
library PoolUnitLib {
    function supply(PoolUnit poolUnit) {
        
    }
}